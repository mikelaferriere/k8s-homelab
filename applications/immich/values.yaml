---
immich:
  image:
    tag: v1.98.2

  immich:
    persistence:
      library:
        existingClaim: nfs-photos

  postgresql:
    metrics:
      enabled: true
    enabled: true

  redis:
    enabled: true

  server:
    service:
      main: 
        annotations:
          metallb.universe.tf/loadBalancerIPs: 10.10.10.201
        type: LoadBalancer

