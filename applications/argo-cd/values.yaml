---
argo-cd:
  dex:
    enabled: false
  notifications:
    enabled: false
  applicationSet:
    enabled: true
  server:
    extraArgs:
      - --insecure
    service:
      type: LoadBalancer

  # global:
  #   podAnnotations:
  #     prometheus.io/scrape: "true"
  #     prometheus.io/path: /metrics
  #     prometheus.io/port: "8080"